# このファイルは requirements.in から uv で生成する。手動編集禁止。
# 再生成コマンド: uv pip compile requirements.in -o requirements.txt

google-cloud-bigquery==3.25.0
google-cloud-logging==3.11.0
functions-framework==3.8.0
requests==2.32.3
